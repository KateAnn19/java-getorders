package local.mcgeeka.getorders31.views;

import java.util.List;

public interface CustomerOrder
{
    String getName(); //no choice on this line name relates to name in repo (custom query)
    int getAmount();
}
